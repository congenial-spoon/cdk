import IconDisplay from '../../../components/cdk-example/IconDisplay'
import Callout from "nextra-theme-docs/callout";

# Icon

语义化的矢量图形。使用图标组件，你需要安装 `@stlt/remixicon-react` 图标组件包：

<Callout emoji="">
  <strong>@stlt/remixicon-react</strong>基于 <strong>[remixicon](https://remixicon.com/)</strong> 构建
</Callout>

<Callout type="warning" emoji="⚠️">
  remixicon 目前处于 <strong> [STOP UPDATING TEMPORARILY 暂停更新](https://github.com/Remix-Design/RemixIcon/issues/232) </strong> 但是目前拥有两千多个图片，够用了。
</Callout>

```bash
npm install --save @stlt/remixicon-react
```

<IconDisplay />
