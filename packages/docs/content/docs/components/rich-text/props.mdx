---
id: rich-text
scope: props
---

## Props

### Rich Text Props

The `RichText` component wraps all its children in the
[Box](/docs/components/layout/box) component, so you can pass all `Box` props to change its
style.

<PropsTable of='RangeSlider' />

### RangeSliderThumb Props

`RangeSliderThumb` composes [Box](/docs/components/layout/box) so you can pass all `Box`
props to change its style.

### RangeSliderFilledTrack Props

`RangeSliderFilledTrack` composes [Box](/docs/components/layout/box) so you can pass all
`Box` props to change its style.

### RangeSliderTrack Props

`RangeSliderTrack` composes [Box](/docs/components/layout/box) so you can pass all `Box`
props to change its style.
